/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package eu.geekplace.beanstalk.app;

import eu.geekplace.beanstalk.list.LinkedList;

import static eu.geekplace.beanstalk.utilities.StringUtils.join;
import static eu.geekplace.beanstalk.utilities.StringUtils.split;
import static eu.geekplace.beanstalk.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
